package com.lucaswing.sliding_tiles_solver.model;

/**
 * Represents a Direction a tile moves on the board.
 * U is up, L is left, D is down, R is right.
 * 
 * @author Luke
 * @version 1.0
 */
public enum Direction {
    U,
    L,
    D,
    R;
}
